const game = document.getElementById("game-container");
const allPictures = []; //Add all picture's href attributes in Array.
let chosenPictures = []; //Randomize pictures and add them in this Array.
